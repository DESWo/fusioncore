node_modules
dist
.DS_Store
*.local

# Playwright run artifacts (traces, reports). The suite itself is tracked in
# tests/e2e; its output is not.
test-results/
playwright-report/

# Pages is built and published by .github/workflows/deploy-pages.yml.
# Never commit a build: the old committed docs/ drifted six days behind source.
docs

# Local agent tooling. Kept on disk so the dev-server launcher and the working
# notes still function locally, but not part of the public repo: they are
# development scaffolding, not something the project ships.
.claude/
CLAUDE.md

# Working backlog. Useful locally, but it is raw development notes: unread-flag
# counts, blunt verdicts on half-finished features, quotes from playtests. That
# is not what the public repo should be presenting. Kept on disk; track the
# real work in GitHub Issues.
FEEDBACK.md
